# keyword_config.py

# Keywords and corresponding plugins for the tool
keyword_plugin_map = {
    "subdomain": {
        "keywords": ["find subdomains", "enumerate subdomains", "subdomain search"],
        "plugin": "subdomain",
        "output_naming": "results_subdomain_{domain}.json"
    }
}
